#ifndef VAYU_SIM_NAVHAL_PORT_DWT_H
#define VAYU_SIM_NAVHAL_PORT_DWT_H
/* Host SITL stub. hal_cycle_counter_get / hal_cycle_counter_init are
 * implemented in host_navhal.c (CLOCK_MONOTONIC scaled to a virtual
 * 84 MHz cycle counter). */
#endif
